<?php
/**
 * Quick view — withdrawn.
 *
 * Products open on their own page again. This snippet is kept only so the
 * import has something to switch off: Site Studio's import is an upsert and
 * never deletes a record, so removing the folder would have left the feature
 * running on the site with no way to reach it from here.
 *
 * Delete the snippet in WordPress and this file goes with it.
 *
 * The code is in the history, at 6d14abf.
 */
