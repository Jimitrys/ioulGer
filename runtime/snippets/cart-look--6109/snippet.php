<?php
/**
 * Cart look — retired.
 *
 * This styled the WooCommerce Cart *Block*. The cart page renders the
 * shortcode cart, so almost none of its 650 lines ever matched anything, and
 * the cart was left looking like stock WooCommerce.
 *
 * The cart is dressed by the "Cart" canvas now, against the markup the page
 * actually prints, with the shell and the controls coming from the shop
 * surface in the global stylesheet.
 *
 * Kept as an empty file rather than deleted: importing never removes a record
 * from WordPress, so the snippet has to be disabled here to be disabled there.
 */
